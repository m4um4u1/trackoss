export const environment = {
  production: true,
  useConfigService: true, // Use runtime configuration service
  baseUrl: '', // Will be loaded from config.json at runtime
  valhallaUrl: '', // Will be loaded from config.json at runtime
};
